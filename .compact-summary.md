## Goal
- Compare the FireRedVAD reference Python implementation to the TypeScript/Node.js rewrite and rectify any discrepancies.

## Progress
### Done
- **ROOT CAUSE IDENTIFIED & FIXED**: Audio normalization. Reference passes int16 values directly to Kaldi (not normalized to [-1, 1]). TS was dividing by 32768, producing features with wrong energy.
- **FIXED**: Removed `/32768` normalization in `pcm16ToFloat32()` in `FireRedVadWrapper.ts:610`.
- **FIXED**: Updated log floor from `1e-10` to `1.192e-7` (Kaldi FLT_MIN) in `FireRedVadWrapper.ts:428`.
- **VERIFIED**: Joe audio produces correct segments `[[104, 249]]`, UH produces `[[91, 246]]`.
- **VERIFIED**: All-at-once features match Kaldi at 1.9e-5 max diff.
- **FIXED**: Pre-existing type errors in `test-model-info.ts` (build now passes).

### Feature Extraction Fixes Applied (all verified)
- HTK mel scale (`1127 * log(1 + f/700)`)
- Mel-space filterbank comparison (no Hz-space normalization)
- No HTK floor, no first-bin-zero fix (Kaldi non-HTK mode)
- Per-frame pre-emphasis (0.97), backward order
- Per-frame DC removal (before pre-emphasis)
- Log floor `1.192e-7` (Kaldi FLT_MIN)
- CMVN: 2 rows, 81 cols, SOS indexing correct
- Ring buffer + init buffer for streaming (first frame needs 400 samples)
- `subarray()` copy to avoid mutation

## Key Facts
- **Audio is NOT normalized to [-1, 1]. Int16 values passed as float32 directly.**
- **Log floor is `1.192e-7` (Kaldi FLT_MIN).**
- FFT and filterbank weights verified correct (max diff 2.6e-14).
- Model file and CMVN file match reference (SHA256 `b3c978...` and `c87f6f...`).

## Relevant Files
- `src/services/audio/FireRedVadWrapper.ts` — main implementation (fixed)
- `src/scripts/test-vad-wrapper.ts` — VAD test (verified correct)
- `src/scripts/test-vad-compare.ts` — feature comparison (matches Kaldi at 1.9e-5)
- `docs/fireredvad-analysis.md` — full pipeline analysis
- `docs/fireredvad-rectification-plan.md` — deep comparison findings
